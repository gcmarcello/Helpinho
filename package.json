{
  "name": "helpinho-monorepo",
  "private": true,
  "scripts": {
    "build": "turbo build",
    "start": "turbo dev",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\""
  },
  "devDependencies": {
    "prettier": "^3.2.5",
    "nodemon": "^3.1.4",
    "turbo": "^2.0.4",
    "typescript": "^5.4.5"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "packageManager": "yarn@1.22.11",
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@repo/config": "*"
  }
}
